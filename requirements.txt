# ── Core framework ──────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.8.2
pydantic-settings>=2.10.1
python-multipart==0.0.9

# ── LangChain / LangGraph ───────────────────────────────────
langchain>=0.4.0
langchain-community>=0.4.0
langchain-ollama>=0.2.0          # FREE local LLM via Ollama
langchain-openai>=0.1.0          # OpenAI GPT models
langchain-anthropic>=0.1.0       # Anthropic Claude models
langchain-google-genai>=0.1.0    # Google Gemini models
langchain-chroma>=0.2.0          # Chroma integration used by app/retriever.py
langgraph>=0.3.0

# ── Vector store ────────────────────────────────────────────
chromadb==0.5.23

# ── PDF ingestion ───────────────────────────────────────────
pypdf==5.0.1
langchain-text-splitters>=0.4.0

# ── Embeddings (free, local via Ollama) ─────────────────────
# No extra package needed — langchain-ollama handles embeddings too

# ── Utilities ────────────────────────────────────────────────
python-dotenv==1.0.1
rich==13.8.0                     # pretty terminal output
httpx==0.27.2                    # for health checks / tests
pytest==8.3.5                    # test runner
