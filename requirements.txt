# ── Core framework ──────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.8.2
pydantic-settings>=2.10.1
python-multipart==0.0.9

# ── LangChain / LangGraph ───────────────────────────────────
langchain==0.2.3
langchain-community==0.2.3
langchain-core==0.2.3
# Removed langchain-ollama and langchain-openai (unnecessary for Gemini deployment)
langchain-google-genai==1.0.4    # Google Gemini models (primary LLM for HuggingFace)
langchain-chroma==0.2.0          # Chroma integration
langgraph==0.2.3

# ── Vector store ────────────────────────────────────────────
chromadb==0.5.23

# ── PDF ingestion ───────────────────────────────────────────
pypdf==5.0.1
# langchain-text-splitters already included in langchain==0.1.14

# ── Embeddings (free, local via Ollama) ─────────────────────
# No extra package needed — langchain-ollama handles embeddings too

# ── Utilities ────────────────────────────────────────────────
python-dotenv==1.0.1
rich==13.8.0                     # pretty terminal output
httpx==0.27.2                    # for health checks / tests
pytest==8.3.5                    # test runner
